{
  "type": {
    "S": "schedule"
  },
  %{ if periods != null ~}
  "periods": {
    "SS": ${ jsonencode(periods) }
  },
  %{ endif ~}
  %{ if override_status != null ~}
  "override_status": {
    "S": "${ override_status }"
  },
  %{ endif ~}
  %{ if timezone != null ~}
  "timezone": {
    "S": "${ timezone }"
  },
  %{ endif ~}
  "description": {
    "S": "${ description }"
  },
  "name": {
    "S": "${ name }"
  }
}
